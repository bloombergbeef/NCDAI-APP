// Заглушки — замените на реальные значения перед релизом.
module.exports = {
  // Адрес бэкенда (ncdai-backend) — телеметрия + проверка обновлений.
  BACKEND_URL: 'http://localhost:4000',

  // Реальный эндпоинт авторизации (логин). Пока не используется — main.js
  // подставляет фейковый успешный ответ вместо запроса сюда (см. ipcMain
  // handler 'auth:login'). Регистрация, в отличие от логина, уже реально
  // ходит на BACKEND_URL/api/auth/register — отдельный LOGIN_API_URL ей не нужен.
  LOGIN_API_URL: 'https://api.ncdai.example/auth/login',

  // Веб-страница, которая грузится в окне после успешного входа.
  // Вся реальная логика/интерфейс программы живёт здесь, на сервере.
  APP_WEB_URL: 'https://app.ncdai.example',
};
